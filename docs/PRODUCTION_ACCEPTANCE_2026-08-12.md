# 生产端到端验收报告（2026-08-12）

## 环境

- 服务：`https://app.freexlib.com`
- 服务进程：`chat2app.service`
- 部署版本：`b107d3f`
- 浏览器验证：已启用
- 浏览器：`/usr/bin/google-chrome`

## 验收样例

应用：极简计数器（`msq9exmhng2f`）

| 步骤 | 请求 | 结果 |
| --- | --- | --- |
| 新建 v1 | 加一、减一、重置、刷新持久化 | 成功；首次完成；Chrome 验证通过 |
| 迭代 v2 | 放大数字、增加深浅主题切换 | 成功；首次完成；完整 HTML 兼容路径 |
| 迭代 v3 | 最近 10 次操作历史、刷新持久化 | 成功；首次完成；命中 Patch；Chrome 验证通过 |
| 回退 | v3 回退到 v2 | 成功；生成 `c000004` 回退记录 |
| 恢复 | 再次增加最近操作历史 | 成功；首次完成；Chrome 验证通过 |

## 验证结果

- 真实 DeepSeek 请求、异步任务、SSE、发布链路均成功。
- 所有生成/迭代均在首次尝试完成，没有任务重试或服务重启。
- Chrome 浏览器验证处于启用状态，版本记录均标记为 `passed`。
- 应用版本、提交父子关系、Diff 摘要、上下文策略和编辑模式均可通过应用详情读取。
- 已验证回退可用，且回退后可以继续在同一会话迭代恢复功能。

## 观察与后续动作

- Patch 策略真实生效，但不是每次都会命中：本轮一条迭代走完整 HTML 兼容路径，一条命中 Patch。
- 下一阶段应持续记录 Patch 命中率、完整 HTML 回退率、浏览器验证失败率和自动修复成功率。
- 当有足够真实失败样本后，再针对性优化 Patch 提示、上下文选择或浏览器交互验收规则。
