{
  "name": "chat2app",
  "version": "1.0.0",
  "private": true,
  "description": "Chat to single-file PWA generation and publishing service",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && node server.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
